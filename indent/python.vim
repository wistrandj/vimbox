let b:did_indent = 1

" Note: Enabling 'smartindent' causes the cursor 
" to jump to first column after inserting the
" comment character '"'

" Note: Enabling 'smartindent' causes:
"   * Comment character '"' has no indent at all (annoying!)
"   * A new block after if-else statement gets one level more
set nosmartindent

" Note: Enabling 'autoindent' keeps the same indention
" level after hitting enter.
set autoindent

