" Allow long lines. Default behavior cuts the line.
set textwidth=0

" Wrap at 'breakat' characters. Not in the middle of a word.
set wrap
set linebreak

" When wrapping is OFF, buffer indicates long lines continuing left/right from
" the screen with these characters.
set listchars+=precedes:<,extends:>

